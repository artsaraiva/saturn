[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "saturn"
version = "0.1.0"
description = "Saturn CLI-first prototype"
requires-python = ">=3.11"
dependencies = ["fastapi", "uvicorn[standard]", "mcp", "prompt_toolkit", "rich", "httpx"]

[project.optional-dependencies]
dev = ["pytest>=8.0", "httpx", "pytest-asyncio"]
sdk = ["httpx"]
all = ["saturn[sdk]"]

[project.scripts]
saturn = "saturn.cli:main"
saturn-mcp = "saturn.daemon.mcp_server:main"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.setuptools.packages.find]
where = ["src"]
